# 📱 মোবাইল ফোন দিয়ে সিগন্যাল বট ব্যবহার এবং ক্লাউডফ্লেয়ার (Cloudflare) বাইপাস গাইড

এই ডকুমেন্টে দেখানো হয়েছে কীভাবে আপনি আপনার মোবাইল ফোন (Android / iOS) দিয়ে এই Quotex অটো-ট্রেডিং সিগন্যাল বটটি নিয়ন্ত্রণ করবেন এবং ক্লাউডফ্লেয়ার চ্যালেঞ্জ কিভাবে বাইপাস করবেন।

---

## 1. 🌐 মোবাইল দিয়ে নিয়ন্ত্রণ করার সেরা উপায়: VPS হোস্টিং (Windows/Linux VPS)
মোবাইলে সরাসরি ব্যাকগ্রাউন্ডে Node.js ও ক্রোম ব্রাউজার চালানো কঠিন এবং ব্যাটারি/ইন্টারনেট কানেকশন অফ হলে বট বন্ধ হয়ে যায়। তাই **সবচেয়ে সেরা ও প্রফেশনাল উপায় হলো একটি কমদামী VPS (Virtual Private Server) ব্যবহার করা**।

### সুবিধা:
- **২৪/৭ সচল থাকবে**: আপনার ফোন বন্ধ থাকলেও বট নিরবচ্ছিন্নভাবে ট্রেড করতে থাকবে।
- **মোবাইল দিয়ে কন্ট্রোল**: একবার VPS-এ বটটি সেটআপ করে দিলে, আপনি আপনার যেকোনো ফোন (iPhone/Android) থেকে সরাসরি **Telegram Chat**-এর মাধ্যমে সব নিয়ন্ত্রণ করতে পারবেন:
  - পেইড ইউজার অ্যাড/রিমুভ করতে পারবেন (`/addpaid <id>`).
  - বট স্ট্যাটাস ও ট্রেড লগ দেখতে পারবেন।
  - পেমেন্ট ভেরিফিকেশন ও অ্যাপ্রুভাল বাটন দিয়ে কন্ট্রোল করতে পারবেন।

### সেটআপ করার ধাপসমূহ:
1. Hostinger, AWS, DigitalOcean বা অন্য কোনো প্রোভাইডার থেকে একটি উইন্ডোজ বা লিনাক্স VPS নিন।
2. VPS-এ Node.js এবং Git ইনস্টল করুন।
3. এই প্রজেক্টটি ক্লোন করে `.env` ফাইলে আপনার টেলিগ্রাম টোকেন, চ্যানেল আইডি এবং Quotex ক্রেডেনশিয়াল সেট করুন।
4. `npm install` রান করুন এবং `npm start` দিয়ে বটটি চালু করুন।
5. ব্যস! এখন আপনার পিসি বা VPS বন্ধ করে দিতে পারেন। আপনি টেলিগ্রাম অ্যাপের অ্যাডমিন প্যানেল দিয়ে ফোন থেকেই সবকিছু নিয়ন্ত্রণ করতে পারবেন।

---

## 2. 🤖 অ্যান্ড্রয়েড ফোনে লোকাল রান করা (Termux ব্যবহার করে)
আপনি যদি কোনো VPS ছাড়া সরাসরি আপনার অ্যান্ড্রয়েড ফোনেই বটটি চালাতে চান, তবে **Termux** অ্যাপ ব্যবহার করতে পারেন।

### সেটআপ করার ধাপসমূহ:
1. গুগল প্লে-স্টোর বা F-Droid থেকে **Termux** অ্যাপটি ইনস্টল করুন।
2. Termux ওপেন করে নিচের কমান্ডগুলো একে একে রান করুন:
   ```bash
   # প্যাকেজ আপডেট করুন
   pkg update && pkg upgrade -y

   # Node.js, Git, Python এবং Chromium ইনস্টল করুন (Puppeteer-এর জন্য)
   pkg install nodejs git python chromium -y
   ```
3. প্রজেক্টটি ক্লোন করুন এবং প্রজেক্ট ডিরেক্টরিতে যান:
   ```bash
   git clone https://github.com/Souravsanyal1/Signal-Telegram-Bot.git
   cd Signal-Telegram-Bot
   ```
4. ডিপেন্ডেন্সি ইনস্টল করুন:
   ```bash
   npm install
   ```
5. Termux-এ Puppeteer চালানোর জন্য পরিবেশ ভেরিয়েবল সেট করতে হবে যেন এটি ইনস্টল করা Chromium ব্যবহার করে:
   ```bash
   export PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
   export PUPPETEER_EXECUTABLE_PATH=$(which chromium)
   ```
6. `.env` ফাইলটি তৈরি বা কনফিগার করুন এবং রান করুন:
   ```bash
   npm start
   ```

---

## 🛡️ 3. ক্লাউডফ্লেয়ার (Cloudflare Turnstile) বাইপাস করার নিয়ম

কোটেক্স (Quotex) অনেক সময় বট ব্লক করার জন্য ক্লাউডফ্লেয়ার ভেরিফিকেশন দেখায়। আমাদের বটটিতে এটি স্বয়ংক্রিয়ভাবে হ্যান্ডেল করার জন্য শক্তিশালী ফিচার দেওয়া হয়েছে:

### ক) অটো-ক্লিকার চ্যালেঞ্জ বাইপাস (Auto-Clicker Bypass)
আমাদের কোডে একটি অটো-ক্লিকার দেওয়া আছে যা ক্লাউডফ্লেয়ারের আইফ্রেম (Iframe) ডিটেক্ট করে এবং স্বয়ংক্রিয়ভাবে **"Verify you are human"** বাটনটিতে ক্লিক করে দেয়।

### খ) স্টিলথ প্লাগইন (Stealth Mode)
বটে `puppeteer-extra-plugin-stealth` এবং বেশ কিছু ক্রোম ফ্ল্যাগ যুক্ত করা হয়েছে। এটি ব্রাউজারের অটোমেশন সিগন্যালগুলো লুকিয়ে রাখে, যার ফলে ক্লাউডফ্লেয়ার বুঝতে পারে না যে এটি একটি রোবট।

### গ) সেশন কুকি সংরক্ষণ (Session Cookies)
আপনি যখন প্রথমবার লগইন করবেন (বা VPS/পিসিতে ক্রোম উইন্ডো ওপেন হবে):
- **একবার লগইন করুন**: প্রথমবার সফলভাবে ইমেইল/পাসওয়ার্ড ও ক্যাপচা সমাধান করে লগইন হয়ে গেলে, সেশনটি `chrome_session` ফোল্ডারে ব্রাউজার কুকি হিসেবে সেভ হয়ে থাকবে।
- **পরবর্তী রান**: পরবর্তীতে বটটি রান করার সময় এটি সরাসরি সেভ করা কুকি লোড করবে। এর ফলে আর লগইন পেজে যেতে হবে না এবং ক্লাউডফ্লেয়ার চ্যালেঞ্জ পুরোপুরি এড়ানো যাবে।
